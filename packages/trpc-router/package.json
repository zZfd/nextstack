{
  "name": "@nextstack/trpc-router",
  "version": "0.0.0",
  "private": true,
  "main": "./index.ts",
  "types": "./index.ts",
  "scripts": {
    "build": "tsc --build",
    "lint": "eslint . --ext ts --max-warnings 0",
    "lint:fix": "eslint . --ext ts --max-warnings 0 --fix",
    "typecheck": "tsc -b"
  },
  "dependencies": {
    "@nextstack/auth": "workspace:*",
    "@nextstack/database": "workspace:*",
    "@nextstack/storage": "workspace:*",
    "@nextstack/validators": "workspace:*",
    "@prisma/client": "^6.15.0",
    "@trpc/server": "^11.5.1",
    "better-auth": "^1.3.9",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@nextstack/eslint-config": "workspace:*",
    "@nextstack/tsconfig": "workspace:*",
    "@types/node": "^20.0.0",
    "typescript": "^5.9.2"
  }
}
